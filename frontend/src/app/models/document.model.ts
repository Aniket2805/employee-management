export interface DocumentMeta {
    id: number
    employeeId: number
    originalFileName: string
    storedFileName: string
    fileType: string
    fileSize: number
    uploadedAt: string
    approved: boolean
}